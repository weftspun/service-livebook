#!/bin/sh
# Livebook starts distribution itself, with longnames and its own EPMD module.
# If the release starts a node first, `Livebook.Application.start_distribution!`
# gets {:already_started, _} and aborts, which is the container failing at boot
# with "Could not start distributed node".
export RELEASE_DISTRIBUTION=none
